{
  "name": "cnodejs",
  "version": "2.0.0",
  "description": "基于electron的cnodejs社区桌面客户端",
  "main": "index.js",
  "scripts": {
    "start": "electron index.js",
    "build:mac": "electron-packager ./ cnodejs --platform=darwin --arch=x64 --icon=icon.icns --overwrite --out ./dist/$npm_package_version --version=0.37.8 --ignore='(.github|.git|.DS_Store|TODOS.md|imgPreview|node_modules)'"
  },
  "devDependencies": {
    "electron-packager": "^7.0.3",
    "electron-prebuilt": "^0.37.0",
    "rimraf": "^2.5.2"
  },
  "dependencies": {}
}
